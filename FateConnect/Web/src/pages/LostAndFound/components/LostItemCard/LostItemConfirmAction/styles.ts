import { styled, Typography } from '@design-system';

/** A frase acompanha o título do diálogo, que é centralizado. */
export const ConfirmationMessage = styled(Typography)({
  textAlign: 'center',
});
