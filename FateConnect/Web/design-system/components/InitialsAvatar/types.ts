/**
 * Porte do círculo. `small` é a marca de identidade no cromo; `large` é o
 * retrato dentro de um diálogo, onde a pessoa é o assunto da tela.
 */
export type InitialsAvatarSize = 'small' | 'large';
