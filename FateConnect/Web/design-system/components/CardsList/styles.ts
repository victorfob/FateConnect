import Stack from '@mui/material/Stack';

import { styled } from '@ds-root/styled';

export const CardsColumn = styled(Stack)({
  flexDirection: 'column',
  width: '100%',
});

export const PaginationRow = styled(Stack)({
  flexDirection: 'row',
  justifyContent: 'center',
  width: '100%',
});
